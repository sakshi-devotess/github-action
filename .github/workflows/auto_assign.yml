name: 'Auto Assign'
on:
  workflow_call:

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v1.2.5
        with:
          configuration-path: 'https://github.com/sakshi-devotess/github-action/blob/main/.github/auto_assign.yml'

